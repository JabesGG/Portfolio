node_modules/
dist/
dist-native/
dist-single/
.parcel-cache/

# Regenerated by `npx cap add android` from capacitor.config.ts. Not committed
# while the app is only prepared for the stores rather than shipped to them —
# once you customise native code (icons, permissions, splash), commit it.
android/
ios/
